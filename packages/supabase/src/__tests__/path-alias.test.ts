describe('@smartodo/supabase path alias', () => {
  it('resolves supabase package correctly', () => {
    // Compile-time check: if this file compiles the alias resolves
    expect(true).toBe(true);
  });
});
